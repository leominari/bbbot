{
  "name": "BBBot",
  "version": "1.0.0",
  "description": "Bot para automatização da stream do BBB.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.ts",
    "dev": "nodemon index.ts"
  },
  "dependencies": {
    "discord.js": "^13.6.0",
    "dotenv": "^14.2.0",
    "nodemon": "^2.0.15",
    "puppeteer": "^13.1.1"
  }
}
